{
  "name": "postiz-backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "dotenv -e ../../.env -- nest start --watch --entryFile=./apps/backend/src/main",
    "build": "cross-env NODE_ENV=production nest build",
    "start": "node --experimental-require-module ./dist/apps/backend/src/main.js", // Removed dotenv to use Railway environment variables directly
    "pm2": "pm2 start pnpm --name backend -- start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
